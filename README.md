
# WZ Guild Tracker — Hyyerr App

This project is fully ready for GitHub → Netlify automatic deployment.

## 🚀 Steps to Deploy (EASY MODE)

### 1. Go to https://github.com and create a new repository
- Click **New**
- Name it anything (e.g., `wz-guild-tracker-hyyerr-app`)
- Leave everything else default
- Create repo

### 2. Upload the project files
- Click **"Upload files"**
- Drag the entire folder contents (NOT the folder itself) into GitHub
- Click **Commit changes**

### 3. Deploy using Netlify
1. Visit https://app.netlify.com
2. Click **"Add new site" → "Import from Git"**
3. Choose **GitHub**
4. Select your repo
5. Build command: `npm run build`
6. Publish directory: `dist`
7. Click **Deploy Site**

Netlify will automatically:
✔ Install dependencies  
✔ Run the Vite build  
✔ Upload `/dist`  
✔ Give you a live URL  

### 4. Done 🎉
Your URL will look like:

```
https://your-site-name.netlify.app
```

### Need help?
Ask ChatGPT to:
- Insert your full component file
- Add features
- Fix bugs
- Customize styles
